# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
supabase/.temp/
*.tsbuildinfo
*.local
.env
.env.*
!.env.example

# Secret backups — never commit, even if a broader rule is relaxed later
.env.supabase-secrets.backup
*.secrets.backup
secrets.json
.supabase-secrets*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local Cursor agent helpers (run-dev.ps1 etc.) — never deploy
.cursor/
